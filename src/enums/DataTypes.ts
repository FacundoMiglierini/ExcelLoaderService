export enum DataTypes {
    STRING = "string",
    NUMBER = "number",
    ARRAY = "array<number>",
}

export enum SchemaDataTypes {
    STRING = "String",
    NUMBER = "Number",
    ARRAY = "Array",
}
