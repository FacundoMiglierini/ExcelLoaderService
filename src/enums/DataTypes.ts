export enum DataTypes {
    STRING = "string",
    NUMBER = "number",
    ARRAY = "array<number>",
}

export enum SchemaDataTypes {
    STRING = "SchemaString",
    NUMBER = "SchemaNumber",
    ARRAY = "SchemaArray",
}
